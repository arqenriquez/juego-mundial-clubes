# Telemetry

The foundation emits possession-change, pass-attempted and pass-completed records with simulation time and actors. It is deliberately independent from commentary and the UI.

Next fields: team, pitch position, pass quality, interception risk, shot xG, sprint distance, pressing count, possession duration, block height and player stamina. A headless batch command will be added only after the tactical engine becomes the authoritative match-result source.
